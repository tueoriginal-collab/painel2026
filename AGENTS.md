# Notas do projeto

Projeto Ghost Copier — Painel. Deploy via GitHub → Netlify (auto-deploy no push
da branch `main`). Migrações de banco em `supabase/migrations/` devem ser
rodadas manualmente no SQL Editor do Supabase.
